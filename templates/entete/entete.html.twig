<nav class="navbar navbar-expand-lg bg-white shadow-sm mb-4 border-bottom">
  <div class="container">
    <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">Mon Blog</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Menu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="mainNavbar">
      <ul class="navbar-nav gap-2">
        <li class="nav-item">
          <a class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active fw-semibold text-primary' : '' }}"
             href="{{ path('app_home') }}">
            <i class="bi bi-house-door me-1"></i> Accueil
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.request.get('_route') == 'app_article_index' ? 'active fw-semibold text-primary' : '' }}"
             href="{{ path('app_article_index') }}">
            <i class="bi bi-file-earmark-text me-1"></i>Articles
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.request.get('_route') == 'app_categorie_index' ? 'active fw-semibold text-primary' : '' }}"
             href="{{ path('app_categorie_index') }}">
            <i class="bi bi-folder2-open me-1"></i> Catégories
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.request.get('_route') == 'app_commentaire_index' ? 'active fw-semibold text-primary' : '' }}"
             href="{{ path('app_commentaire_index') }}">
            <i class="bi bi-chat-left-dots me-1"></i> Commentaires
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>
